import styled from "styled-components";

const basis = {
  mode: {
    light: "#FFFFFF",
    dark: "#1E1E1E",
  },
  line: {
    color: { light: "#E1E1E1", medium: "#D3D3D3", heavy: "#686868" },
    size: { light: "1px", medium: "1px", heavy: "3px" },
  },
};

const theme = {
  basis,
};

export default theme;
